name: hello-world

on:
  push:
    branches: ["stage"]

env:
  TEXT: "Verny Umaña Sánchez"

jobs:
  print: 
    runs-on: ubuntu-latest 

    steps:
      - name: print-to-screen
        run: echo "Hello World! ${{ env.TEXT }}"